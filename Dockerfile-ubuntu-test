FROM openjdk:11

MAINTAINER envomp@taltech.ee

ENV JAVA_OPTS=""

ARG GITLAB_PASSWORD
ENV GITLAB_PASSWORD=$GITLAB_PASSWORD

ARG ARETE_HOME
ENV ARETE_HOME=$ARETE_HOME

ADD . $ARETE_HOME
WORKDIR $ARETE_HOME

# Run Maven build
RUN chmod +x mvnw && ./mvnw test
